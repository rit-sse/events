machine:
  node:
    version: 5.5.0

# We'll write tests one day...for now we'll just run the linter
test:
  override:
    - npm run lint
    - npm run build

deployment:
  master:
    branch: devlop
    owner: rit-sse
    commands:
      - rsync -r dist/ timbrook@ssedev.se.rit.edu:/var/www/events/
