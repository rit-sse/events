machine:
  node:
    version: 6.4.0

# We'll write tests one day...for now we'll just run the linter
test:
  override:
    - npm run lint
    - npm run build

deployment:
  master:
    branch: devlop
    owner: rit-sse
    commands:
      - rsync -r dist/ deploy@ssedev.se.rit.edu:/var/www/events/
